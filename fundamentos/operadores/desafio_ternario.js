

const a = 8;
const b = 4;
operacao = "/";

const resultado =
    operacao === "+" ? a + b :
        operacao === "-" ? a - b :
            operacao === "*" ? a * b : a / b;

// const saudacao = hoara <= 12 ? "Bom dia!" : hoara <= 18 ? "Bom tarde!" : "Boa noite!";

console.log(resultado);